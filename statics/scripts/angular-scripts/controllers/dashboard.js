define(function () {
    'use strict';

    var app = angular.module('app');

    app.controller('LogsListController', function LogsListController(
        $http,
        $filter,
        $scope,
        toastr,
        NgTableParams,
        cmsService,
        $state,
        $timeout
    ) {});
});
